# Implementation-of-Own-atoi-and-atof
Custom implementation of atoi() and atof() functions in C without using built-in conversion functions. Handles whitespace, signs, integers, decimal numbers, and invalid characters.
